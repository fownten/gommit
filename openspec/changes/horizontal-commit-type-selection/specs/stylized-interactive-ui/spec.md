## MODIFIED Requirements

### Requirement: Interactive Type Selection
The system SHALL provide an interactive horizontal selection row (using Bubble Tea) for the user to select the commit type (feat, fix, chore).

#### Scenario: Navigating the row
- **WHEN** the user presses Left, Right, H, L, or Tab keys
- **THEN** the highlighted selection SHALL move horizontally accordingly

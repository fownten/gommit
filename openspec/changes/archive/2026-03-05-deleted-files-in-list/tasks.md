## 1. Implementation

- [x] 1.1 Add a test case to `internal/git/status_test.go` for deleted files.
- [x] 1.2 Verify and fix `internal/git/status.go` to handle `D` and `R` statuses.
- [x] 1.3 Ensure path parsing handles renames if `R` is encountered.

## 2. Verification

- [x] 2.1 Manually verify by staging a deletion and running `./gommit`.

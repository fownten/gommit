## 1. Implementation
- [x] 1.1 Update message formatting in `main.go` to iterate over `staged` files.

## 2. Verify
- [x] 2.1 Run `go run main.go` and verify the file list appears in the "Generated commit message" section.
